Thank you for downlading this totally safe program
To run this program, please make sure python is downloaded
If not, go to https://www.python.org/downloads/ and download the latest version

Then, you need to check if pygame is installed
To do this, open a terminal on windows and type "pip list" and press enter
Try finding a package named "pygame" in the output

If pygame is not installed
Type "pip install pygame" into the command line and press enter

Congratulations, you can now run the program
Make sure the directory for the terminal is in the folder above this open
The directory should have 3 folders, Files, Saves and INSTRUCTIONS
Open terminal in that folder or locate to that folder through terminal
Type "python ./Files/Powder.py" into terminal
Press enter

The game should now run
Now please read the other info text file